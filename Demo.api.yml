
spring:
  boot:
    admin:
       client:
         url:http://localhost:2222

management:
  endpoints:
    web:
      exposure:
        include: "*"

message:"This is a test message from config server"
         
